schema_version = "1.0.0"

enabled_tools = [
  "gitleaks",
  "cargo audit",
  "npm audit"
]

required_tools = [
  "gitleaks"
]

advisory_tools = [
  "cargo audit",
  "npm audit"
]

[severity_thresholds]
fail_lane_on = "high"
