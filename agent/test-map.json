{
  "workspace": "humanlint",
  "tests": {
    "AGENTS.md": {
      "command": "cargo run -p humanlint -- . --json repo-score.json --md repo-score.md",
      "purpose": "verify agent instruction routing is visible to the audit"
    },
    "agent/": {
      "command": "cargo run -p humanlint -- . --json repo-score.json --md repo-score.md",
      "purpose": "verify machine-readable maps and policy remain scannable"
    },
    "docs/": {
      "command": "cargo run -p humanlint -- . --json repo-score.json --md repo-score.md",
      "purpose": "verify docs remain agent-readable and routed"
    },
    "crates/humanlint/": {
      "command": "cargo test -p humanlint && cargo run -p humanlint -- versions",
      "purpose": "verify the audit package compiles and still emits JSON/Markdown"
    },
    "package.json": {
      "command": "npm ci && npm --workspace @humanlint/ux-qa run build && npm --workspace @humanlint/ux-qa run test",
      "purpose": "verify the npm workspace and UX QA runtime"
    },
    "packages/ux-qa/": {
      "command": "npm --workspace @humanlint/ux-qa run build && npm --workspace @humanlint/ux-qa run test",
      "purpose": "verify the Playwright geometry runtime"
    },
    "paper/humanlint.tex": {
      "command": "latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=paper paper/humanlint.tex",
      "purpose": "build the paper"
    },
    "assets/": {
      "command": "latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=paper paper/humanlint.tex",
      "purpose": "verify paper image assets render"
    },
    "paper/": {
      "command": "latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=paper paper/humanlint.tex",
      "purpose": "build the paper"
    },
    "tools/": {
      "command": "cargo run -p humanlint -- . --json repo-score.json --md repo-score.md",
      "purpose": "score the workspace"
    }
  }
}
