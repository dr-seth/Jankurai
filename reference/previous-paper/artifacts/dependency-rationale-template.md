# Dependency Rationale Template

Use one entry per non-trivial dependency. Keep entries short enough that an agent can read them during dependency or security work.

## `<crate-name>`

- Owner:
- Purpose:
- Runtime path:
- Feature flags enabled:
- Why this dependency instead of local code:
- Security posture:
- Update policy:
- Proof commands affected:
- Removal or replacement trigger:

