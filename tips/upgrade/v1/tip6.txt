I studied the archive closely. My read: **the thesis is strong, and the Rust audit + UX QA package are a very good nucleus, but the project is not yet “standard-ready” because the install path, evidence model, and CI proof loop are not hard enough.** The biggest opportunity is to turn Humanlint from a paper + repo scanner into an **agent passport**: one command installs the standard, writes the right agent-help files, configures CI, creates actionable repair queues, and gives every agent IDE the same repo contract.

A few concrete breakpoints I found in the provided archive:

| Area             | Issue                                                                                                                                                                                                        | Why it matters                                                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Installability   | No `Cargo.lock` or `package-lock.json` in the source archive. `npm ci` fails immediately; `cargo install --locked`/locked install story is inconsistent.                                                     | The first-run experience must never fail on missing lockfiles.                                                                              |
| Paper build      | `latexmk` fails because `paper/tex/frontmatter.tex` references missing `assets/humanlint_header.png`. README also references missing assets.                                                                 | A “standard” cannot ship with broken reproducible docs.                                                                                     |
| Release hygiene  | Archive contains many AppleDouble `._*` files.                                                                                                                                                               | Signals weak packaging; can pollute audits and confuse agents.                                                                              |
| CI security lane | Workflow currently echoes tools like `gitleaks`, `syft`, `grype`, `zizmor` rather than running them.                                                                                                         | The audit can mistake “tool name mentioned” for proof. This is exactly the kind of vibe-coding false-green the project is meant to prevent. |
| Audit evidence   | Several checks scan all text, so docs or the audit source itself can satisfy operational requirements. Example: UX/web evidence can be satisfied by mentioning React/Vite/Storybook in docs or scanner code. | The audit needs scoped evidence: docs are not proof, real commands/artifacts are proof.                                                     |
| Reporting        | JSON/Markdown reports exist, but they do not yet produce a stable repair queue with rule IDs, exact commands, owner/lane, exception template, SARIF, ratchet, or issue sync.                                 | Agents need the report to be directly executable.                                                                                           |
| Productization   | There is no `humanlint init` or installer that writes Codex/Cursor/Claude/Gemini/Copilot/Antigravity adapter files.                                                                                          | Adoption dies unless setup is one-command.                                                                                                  |

Also, I could not run Cargo tests in this environment because `cargo`/`rustc` are not installed here. I did run the available Node/paper checks: `npm ci` failed due missing lockfile, and `latexmk` failed due the missing header asset.

Below are the **best 3 upgrades** I would make. These are intentionally large enough to change the project’s trajectory, but bounded enough that an agent could implement them.

---

# Upgrade 1 — **Agent Passport Installer**

## Goal

Make Humanlint installable in one command:

```bash
humanlint init . --yes --tools all --ci github --mode advisory
humanlint audit . --md repo-score.md --json repo-score.json
```

This should create the repo’s **Agent Passport**: the canonical contract that every coding agent and IDE reads before touching code.

This aligns with where agent tooling is going. Codex reads `AGENTS.md` files globally and per project; Gemini uses hierarchical `GEMINI.md`; GitHub Copilot supports repo/path instructions plus `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md`; and SKILL-style bundles are becoming a portable primitive for agent capabilities. ([OpenAI Developers][1])

## What it should do

Add a new CLI subcommand:

```bash
humanlint init [repo]
  --yes
  --dry-run
  --force
  --mode advisory|standard|strict
  --stack rust-ts-vite-react-postgres
  --tools codex,cursor,claude,gemini,copilot,antigravity,all
  --ci github|none
  --issue-tracker beads|github|none
  --ux-qa
  --apply-moves
```

Default behavior should be safe:

```bash
humanlint init .
```

should inspect the repo, produce a plan, and ask only when ambiguity matters. In noninteractive mode, it should create the minimum safe files and mark unresolved items as TODOs.

## Files it should create or update

### Canonical Humanlint files

```text
AGENTS.md
agent/HUMANLINT_STANDARD.md
agent/humanlint.toml
agent/audit-policy.toml
agent/owner-map.json
agent/test-map.json
agent/generated-zones.toml
agent/proof-lanes.toml
agent/repo-score.json
agent/repo-score.md
docs/decisions/0001-adopt-humanlint.md
docs/exceptions/.gitkeep
```

### Tool-specific adapters

```text
# Codex
AGENTS.md
apps/web/AGENTS.md                       # optional scoped override
crates/*/AGENTS.md                       # optional scoped override

# Cursor
.cursor/rules/humanlint-core.mdc
.cursor/rules/humanlint-web.mdc
.cursor/rules/humanlint-rust.mdc

# Claude
CLAUDE.md
.claude/skills/humanlint-audit/SKILL.md

# Gemini
GEMINI.md

# GitHub Copilot
.github/copilot-instructions.md
.github/instructions/humanlint.instructions.md
.github/skills/humanlint-audit/SKILL.md

# Portable / future agents
.agents/skills/humanlint-audit/SKILL.md
.agents/skills/humanlint-ux-qa/SKILL.md
.agents/skills/humanlint-init/SKILL.md

# Antigravity-style adapter
.agent/rules/humanlint.md
.agent/workflows/humanlint-audit.md
```

GitHub’s current skill docs explicitly support project skills under `.github/skills`, `.claude/skills`, or `.agents/skills`, with `SKILL.md` as the required manifest file. ([GitHub Docs][2])

## What `AGENTS.md` should look like

Root `AGENTS.md` should be short. It should not duplicate the full standard.

```md
# AGENTS.md

This repository follows Humanlint.

Read these before making changes:

1. `agent/HUMANLINT_STANDARD.md`
2. `agent/humanlint.toml`
3. `agent/owner-map.json`
4. `agent/test-map.json`
5. `agent/generated-zones.toml`
6. `agent/proof-lanes.toml`

Before editing:
- Identify the touched owner and proof lane.
- Do not edit generated files.
- Do not move durable product truth out of Rust/PostgreSQL.
- Do not add Python product truth.
- Do not change UI without running the relevant UX proof lane.

Before final response:
- Run `humanlint audit . --changed`.
- Report score changes, hard findings, and proof commands run.
```

## Best-known stack boundary file

Add:

```text
docs/stack-boundaries.md
```

With this canonical table:

| Layer                     | Owns                                                                                                            | Must not own                                                                           |
| ------------------------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Rust domain crates        | Durable product behavior, invariants, authorization policy, typed errors, idempotency, command/query boundaries | Browser UI, handwritten generated DTOs, direct framework leakage into domain           |
| Rust application/adapters | HTTP/RPC handlers, persistence adapters, workers, external service clients, transaction boundaries              | Business rules hidden in handlers, direct DB access from UI, untyped JSON soup         |
| PostgreSQL                | Durable state, constraints, indexes, migrations, referential integrity, RLS where applicable                    | App-only invariants that should be constrained durably                                 |
| TypeScript/React/Vite     | Browser UI, local interaction, rendering, accessibility, form state, generated API clients                      | Durable product truth, authorization, direct DB access, handwritten backend DTOs       |
| Contracts                 | API schemas, generated clients, generated bindings                                                              | Manual edits in generated outputs                                                      |
| Python                    | ML/data/eval/model-serving islands only                                                                         | Product truth, authorization, primary business workflows, direct production DB control |

This is the anti-scatter rule. The standard should say: **Rust and PostgreSQL own truth; TypeScript owns presentation; Python owns ML/data islands only; contracts connect them.**

## Repo organization repair

`humanlint init` should detect common messy layouts and produce a migration plan:

```text
docs/migration/humanlint-plan.md
agent/humanlint-init-plan.json
```

Example findings:

```json
{
  "finding_id": "HL-INIT-DB-001",
  "severity": "high",
  "message": "Database migration files are split across 3 directories.",
  "recommended_layout": "db/migrations",
  "safe_action": "Create db/migrations and document move plan.",
  "requires_confirmation": true
}
```

Do not silently move large trees. Instead:

```bash
humanlint init . --apply-moves
```

should be required for actual file moves.

## Rust implementation steering

Add:

```text
crates/humanlint/src/init/mod.rs
crates/humanlint/src/init/detect.rs
crates/humanlint/src/init/planner.rs
crates/humanlint/src/init/templates.rs
crates/humanlint/src/init/writer.rs
crates/humanlint/src/init/adapters.rs
crates/humanlint/src/init/issues.rs
crates/humanlint/src/templates/
```

Update:

```text
crates/humanlint/src/main.rs
crates/humanlint/src/model.rs
crates/humanlint/src/render.rs
Cargo.toml
README.md
AGENTS.md
docs/agent-native-standard.md
paper/humanlint.md
```

Suggested structs:

```rust
pub struct InitOptions {
    pub yes: bool,
    pub dry_run: bool,
    pub force: bool,
    pub mode: InitMode,
    pub stack: StackProfile,
    pub tools: Vec<AgentTool>,
    pub ci: CiProvider,
    pub issue_tracker: IssueTracker,
    pub ux_qa: bool,
    pub apply_moves: bool,
}

pub struct InitPlan {
    pub repo_root: PathBuf,
    pub detected: DetectedRepo,
    pub writes: Vec<FileWrite>,
    pub patches: Vec<FilePatch>,
    pub moves: Vec<PlannedMove>,
    pub warnings: Vec<InitWarning>,
}
```

## Tests to add

```text
crates/humanlint/tests/init_smoke.rs
crates/humanlint/tests/init_idempotent.rs
crates/humanlint/tests/init_dry_run.rs
crates/humanlint/tests/init_existing_agents.rs
crates/humanlint/tests/init_tool_adapters.rs
crates/humanlint/tests/init_repo_detection.rs
crates/humanlint/tests/init_lockfiles.rs
```

Test cases:

1. Empty repo gets minimal passport.
2. Existing `AGENTS.md` gets a Humanlint pointer, not overwritten.
3. `--dry-run` writes nothing.
4. `--tools all` creates every adapter.
5. Missing lockfiles are flagged.
6. Vite app is detected.
7. Rust workspace is detected.
8. Python product code outside `python/ml` or `tools` is flagged.
9. Generated zones are initialized and protected.
10. Re-running `init` is idempotent.

## Acceptance criteria

A user should be able to do this in a random repo:

```bash
humanlint init . --yes --tools all --ci github
humanlint audit . --mode advisory
```

and receive:

```text
AGENTS.md installed
Agent adapters installed
CI workflow installed
Owner/test maps initialized
UX QA optional lane installed
Audit completed
Repair queue written
```

---

# Upgrade 2 — **Proof Graph Auditor**

## Goal

Replace broad string scanning with a scoped, graph-backed evidence model.

Right now, the audit is promising but too easy to fool. It can treat documentation mentions as operational evidence. The new rule should be:

> **Docs can explain proof. Only executed commands, configs, lockfiles, generated reports, source topology, and declared maps can satisfy proof.**

This is the core anti-vibe-coding upgrade.

## New audit model

Add four evidence scopes:

```rust
pub enum EvidenceScope {
    RuntimeSource,
    OperationalConfig,
    GeneratedArtifact,
    Documentation,
}
```

Then make every rule declare which scopes can satisfy it.

Examples:

| Rule                         | Valid evidence                                                                           | Invalid evidence                        |
| ---------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------- |
| UX QA exists                 | `ux-qa.config.*`, Playwright report, CI step that runs UX QA                             | README saying “we use UX QA”            |
| Security lane exists         | Workflow step actually invoking `gitleaks`, `cargo audit`, `osv-scanner`, `zizmor`, etc. | `echo "run gitleaks"`                   |
| Web surface exists           | `package.json` deps, `vite.config.*`, `src/**/*.tsx`                                     | Paper mentioning React                  |
| Rust proof exists            | `Cargo.lock`, `cargo nextest`, Rust tests, CI step                                       | Docs saying “run cargo test”            |
| Generated contract protected | declared generated zone + file hashes or generation commands                             | Folder name containing `generated` only |

## Hard checks vs soft checks

Make policy explicit:

```toml
# agent/audit-policy.toml

mode = "standard"
fail_below = 85
fail_on_hard_findings = true
ratchet = true

[hard]
secrets = true
generated_mutation = true
missing_agent_passport = true
missing_fast_lane = true
missing_owner_map = true
missing_test_map = true
missing_lockfile = true
python_product_truth = true
db_wrong_layer = true
destructive_migration_without_safety = true
prompt_injection_tool_policy = true
security_lane_missing = true

[soft]
naming_fog = true
large_file_warning = true
todo_density = true
fallback_soup = true
weak_docs = true
missing_storybook_for_noncritical_ui = true
```

Hard checks should fail `standard` and `release` mode. Soft checks should affect score, ratchet, and repair queue, but not necessarily block early adoption.

## New CLI modes

```bash
humanlint audit .
humanlint audit . --changed
humanlint audit . --changed-from origin/main
humanlint audit . --mode advisory
humanlint audit . --mode standard
humanlint audit . --mode ratchet
humanlint audit . --mode release
humanlint audit . --sarif humanlint.sarif
humanlint audit . --github-step-summary
humanlint audit . --beads
humanlint audit . --github-issues
```

Modes:

| Mode       | Behavior                                                                    |
| ---------- | --------------------------------------------------------------------------- |
| `advisory` | Never fails, writes report and queue. Best for first adoption.              |
| `standard` | Fails on hard findings or score below threshold.                            |
| `ratchet`  | Allows existing debt but blocks regressions.                                |
| `release`  | Strictest mode; requires clean proof lanes and no unresolved hard findings. |

## Report schema upgrade

Current reports need to become agent-executable.

Add:

```text
schemas/humanlint-report.schema.json
schemas/humanlint-finding.schema.json
```

Every finding should include:

```json
{
  "finding_id": "hl_01j...",
  "rule_id": "HLT-SEC-001",
  "title": "Security lane is declared but not executed",
  "severity": "high",
  "hardness": "hard",
  "confidence": 0.96,
  "path": ".github/workflows/humanlint-audit.yml",
  "line": 42,
  "owner": "platform",
  "proof_lane": "security",
  "evidence_scope": "OperationalConfig",
  "why_it_matters": "Echoed tool names are not proof.",
  "agent_fix": "Replace echo placeholders with real tool installation and execution.",
  "validation_command": "humanlint audit . --mode standard",
  "exception_template": "docs/exceptions/HLT-SEC-001.md",
  "autofixable": false,
  "issue_refs": [],
  "bead_refs": []
}
```

Also add:

```json
{
  "score": {
    "raw": 72,
    "final": 72,
    "previous": 70,
    "delta": 2,
    "ratchet_passed": true
  },
  "policy": {
    "mode": "standard",
    "fail_below": 85
  },
  "repair_queue": [
    {
      "rank": 1,
      "finding_id": "hl_...",
      "next_command": "humanlint init . --repair HLT-SEC-001"
    }
  ]
}
```

## Issue tracking integration

Add:

```bash
humanlint issues sync --backend beads
humanlint issues sync --backend github
humanlint issues export --jsonl repair-queue.jsonl
```

Beads is a strong fit because it is a distributed graph issue tracker designed for coding agents, with dependency-aware issue structure and persistent local project memory. ([GitHub][3])

Humanlint should not require Beads, though. It should always write:

```text
agent/repair-queue.jsonl
agent/repair-queue.md
```

Then Beads/GitHub/Linear/Jira integrations can consume the same stable queue.

Stable finding IDs should be deterministic:

```text
blake3(rule_id + normalized_path + normalized_evidence + first_line)
```

That lets findings survive across CI runs and prevents duplicate issues.

## Rust implementation steering

Refactor audit into modules:

```text
crates/humanlint/src/audit/mod.rs
crates/humanlint/src/audit/context.rs
crates/humanlint/src/audit/scope.rs
crates/humanlint/src/audit/policy.rs
crates/humanlint/src/audit/rules.rs
crates/humanlint/src/audit/report.rs
crates/humanlint/src/audit/ratchet.rs
crates/humanlint/src/audit/issues.rs
crates/humanlint/src/audit/sarif.rs

crates/humanlint/src/audit/detectors/agents.rs
crates/humanlint/src/audit/detectors/ci.rs
crates/humanlint/src/audit/detectors/security.rs
crates/humanlint/src/audit/detectors/rust.rs
crates/humanlint/src/audit/detectors/web.rs
crates/humanlint/src/audit/detectors/python.rs
crates/humanlint/src/audit/detectors/db.rs
crates/humanlint/src/audit/detectors/contracts.rs
crates/humanlint/src/audit/detectors/ux.rs
crates/humanlint/src/audit/detectors/generated.rs
crates/humanlint/src/audit/detectors/naming.rs

crates/humanlint/src/graph/mod.rs
crates/humanlint/src/graph/files.rs
crates/humanlint/src/graph/imports.rs
crates/humanlint/src/graph/owners.rs
crates/humanlint/src/graph/proof_lanes.rs
```

Recommended crates:

```toml
[dependencies]
clap = { version = "...", features = ["derive"] }
serde = { version = "...", features = ["derive"] }
serde_json = "..."
toml = "..."
ignore = "..."
globset = "..."
regex = "..."
blake3 = "..."
schemars = "..."
camino = "..."
miette = "..."
thiserror = "..."
rayon = "..."
```

For richer code understanding:

```toml
tree-sitter = "..."
tree-sitter-rust = "..."
tree-sitter-typescript = "..."
tree-sitter-javascript = "..."
tree-sitter-python = "..."
cargo_metadata = "..."
```

Do not overdo AST on day one. The first win is **evidence scoping**. AST can come after.

## External Rust/security proof tools

The standard should recommend these tools as optional lanes, not as required dependencies for the Humanlint binary:

| Tool                           | Why                                                                                                  |
| ------------------------------ | ---------------------------------------------------------------------------------------------------- |
| `cargo-nextest`                | Faster Rust test runner with test selection and retry/slow-test controls. ([Nexte][4])               |
| `cargo-audit`                  | Checks `Cargo.lock` against RustSec advisories. ([Rust Blog][5])                                     |
| `cargo-deny`                   | Policy checks for advisories, licenses, sources, and dependency policy. ([Crates.io][6])             |
| `cargo-vet`                    | Tracks audited dependency trust and reusable audits. ([Mozilla GitHub][7])                           |
| `cargo-machete`                | Fast unused dependency detection, with explicit allowance for false-positive metadata. ([GitHub][8]) |
| `cargo-geiger`                 | Reports unsafe Rust usage statistics. ([GitHub][9])                                                  |
| `cargo-semver-checks`          | Public API drift for libraries.                                                                      |
| `zizmor` + `actionlint`        | GitHub Actions security/linting.                                                                     |
| `gitleaks` or `trufflehog`     | Secret scanning.                                                                                     |
| `osv-scanner`, `syft`, `grype` | Vulnerability/SBOM/container/dependency lanes.                                                       |

The audit should verify either:

1. the tool was actually run in CI, or
2. a current artifact exists, or
3. an explicit exception exists.

It should never accept a string mention as proof.

## Tests to add

Add fixture repos:

```text
crates/humanlint/tests/fixtures/minimal_pass/
crates/humanlint/tests/fixtures/docs_only_false_positive/
crates/humanlint/tests/fixtures/echo_security_false_positive/
crates/humanlint/tests/fixtures/missing_lockfile/
crates/humanlint/tests/fixtures/web_without_ux/
crates/humanlint/tests/fixtures/generated_zone_hand_edit/
crates/humanlint/tests/fixtures/python_product_truth/
crates/humanlint/tests/fixtures/db_wrong_layer/
crates/humanlint/tests/fixtures/destructive_migration/
crates/humanlint/tests/fixtures/beads_existing/
```

Add tests:

```text
crates/humanlint/tests/audit_scope.rs
crates/humanlint/tests/audit_policy.rs
crates/humanlint/tests/audit_sarif.rs
crates/humanlint/tests/audit_ratchet.rs
crates/humanlint/tests/audit_issue_queue.rs
crates/humanlint/tests/audit_security_lane.rs
crates/humanlint/tests/audit_ux_lane.rs
crates/humanlint/tests/audit_generated_zones.rs
```

Must-have regression tests:

1. README saying “we use Storybook” must not satisfy UX QA.
2. Workflow line `echo "gitleaks"` must not satisfy secret scanning.
3. `package-lock.json` missing must fail NPM CI readiness.
4. `Cargo.lock` missing must fail Rust audit readiness for apps.
5. AppleDouble files are ignored or flagged as packaging junk.
6. Generated contract hand edit is hard failure.
7. Python durable product logic outside allowed islands is hard failure.
8. Direct DB access from React/TS UI is hard failure.
9. Docs-only evidence never satisfies operational proof.
10. SARIF output validates against schema.

## Acceptance criteria

The current provided archive should no longer produce a quiet/stale score. It should flag at least:

```text
Missing package-lock.json
Missing Cargo.lock or inconsistent locked install story
Paper asset missing
Security lane uses echo placeholders
Docs/tool-code evidence cannot satisfy UX proof
AppleDouble files in release archive
No humanlint init installer
No SARIF / ratchet / repair queue
```

That is good. The standard becomes trustworthy when it can honestly critique itself.

---

# Upgrade 3 — **Proofpack CI + UX QA Runtime**

## Goal

Make the standard effortless to run in CI and make the UX QA tester a first-class proof lane, not a side package.

The target experience:

```yaml
- uses: humanlint/humanlint-action@v1
  with:
    mode: ratchet
    changed-only: true
    run-ux-qa: true
```

and locally:

```bash
humanlint audit . --changed
humanlint ux discover
humanlint ux audit --config ux-qa.config.toml
```

## GitHub Action

Add:

```text
action.yml
.github/workflows/release.yml
.github/workflows/humanlint.yml
```

`action.yml` inputs:

```yaml
inputs:
  mode:
    default: ratchet
  fail-below:
    default: "85"
  changed-only:
    default: "true"
  run-ux-qa:
    default: "false"
  ux-config:
    default: "ux-qa.config.toml"
  issue-backend:
    default: "none"
  sarif:
    default: "true"
  github-step-summary:
    default: "true"
```

Outputs:

```yaml
outputs:
  score:
  hard_findings:
  soft_findings:
  report_json:
  report_md:
  sarif:
```

The action should prefer a prebuilt release binary. Do not make every adopter compile the Rust CLI from source.

## CI workflow template

Generated by `humanlint init --ci github`:

```yaml
name: Humanlint

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write
  pull-requests: write
  issues: write

jobs:
  humanlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Humanlint audit
        uses: humanlint/humanlint-action@v1
        with:
          mode: ratchet
          changed-only: true
          run-ux-qa: true
          sarif: true
```

For this repo itself, the workflow must also run real checks:

```bash
cargo test --workspace
cargo nextest run --workspace
cargo audit
cargo deny check
cargo machete
cargo geiger
npm ci
npm test --workspace packages/ux-qa
npm run build --workspace packages/ux-qa
latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=paper paper/humanlint.tex
```

No more echo placeholders.

## UX QA productization

The UX QA package is promising. It already has Playwright-based geometry/focus/layout checks. The upgrade is to make it configurable, discoverable, and integrated into the main audit.

Add:

```text
packages/ux-qa/src/config.ts
packages/ux-qa/src/discover.ts
packages/ux-qa/src/storybook.ts
packages/ux-qa/src/server.ts
packages/ux-qa/src/reporters/sarif.ts
packages/ux-qa/src/reporters/junit.ts
packages/ux-qa/src/reporters/markdown.ts
packages/ux-qa/schemas/ux-qa-report.schema.json
ux-qa.config.toml
```

Example config:

```toml
[server]
command = "npm run preview -- --host 127.0.0.1"
url = "http://127.0.0.1:4173"
wait_on = "http://127.0.0.1:4173"

[defaults]
viewports = ["390x844", "768x1024", "1440x900"]
artifacts = ["screenshot", "aria", "json"]

[[route]]
id = "home"
url = "/"
critical = true

[[route]]
id = "dashboard"
url = "/dashboard"
critical = true

[[route]]
id = "settings"
url = "/settings"
critical = false
```

New commands:

```bash
humanlint ux init
humanlint ux discover
humanlint ux audit --config ux-qa.config.toml
humanlint ux audit --url http://localhost:4173 --viewport 390x844
humanlint ux audit --storybook
```

## UX hard/soft rules

Hard UX checks for critical routes:

```text
No invisible focused element
No unlabeled form controls
No critical button below minimum target size
No horizontal overflow at mobile viewport
No sticky obstruction of primary action
No clipped primary text
No overlapping interactive targets
```

Soft UX checks:

```text
Noncritical target-size warnings
Minor wrapping warnings
Nested scroll warnings
Z-index token warnings
Low-confidence overlap warnings
```

The current package uses a 24px minimum target. Keep 24px as a lower-level pointer minimum, but add a mobile/critical-control policy that defaults to 44px unless an exception is declared.

## UX report integration

Humanlint’s main report should include UX results:

```json
{
  "proof_lanes": {
    "ux": {
      "status": "failed",
      "routes_checked": 4,
      "viewports": ["390x844", "1440x900"],
      "artifacts_dir": "artifacts/ux-qa",
      "findings": [
        {
          "rule_id": "HLT-UX-FOCUS-001",
          "route": "dashboard",
          "viewport": "390x844",
          "selector": "[data-testid='save-button']",
          "screenshot": "artifacts/ux-qa/dashboard-390x844.png"
        }
      ]
    }
  }
}
```

Agents need screenshots, selectors, and one command to reproduce.

## Release/package hygiene

Add a source release script:

```text
xtask/src/main.rs
scripts/package-source.sh
```

It should exclude:

```text
._*
.DS_Store
target/
node_modules/
dist/
test-results/
playwright-report/
*.log
```

Fix the paper build by either including the assets or making the TeX resilient:

```tex
\IfFileExists{assets/humanlint_header.png}{
  \includegraphics[width=\linewidth]{assets/humanlint_header.png}
}{
  \fbox{\Large Humanlint}
}
```

Add both lockfiles:

```text
Cargo.lock
package-lock.json
```

Then update CI to prove the release archive:

```bash
scripts/package-source.sh
tar -xzf dist/humanlint-source.tar.gz -C /tmp/humanlint-smoke
cd /tmp/humanlint-smoke
npm ci
cargo test --workspace
latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=paper paper/humanlint.tex
humanlint audit . --mode advisory
```

## Tests to add

```text
packages/ux-qa/tests/config.spec.ts
packages/ux-qa/tests/discover.spec.ts
packages/ux-qa/tests/server.spec.ts
packages/ux-qa/tests/storybook.spec.ts
packages/ux-qa/tests/reporters.spec.ts
packages/ux-qa/tests/mobile-targets.spec.ts

crates/humanlint/tests/ci_template.rs
crates/humanlint/tests/release_archive.rs
crates/humanlint/tests/ux_report_integration.rs
```

Must-have tests:

1. `ux-qa.config.toml` parses.
2. Multiple routes run.
3. Multiple viewports run.
4. Artifacts are written deterministically.
5. JUnit/SARIF/Markdown reporters work.
6. Storybook discovery works when story index exists.
7. UX report merges into Humanlint report.
8. Generated GitHub workflow contains real commands, not echoes.
9. Release archive contains no `._*`.
10. Source archive paper build does not fail on missing image.

## Acceptance criteria

A fresh Vite/React repo should be able to run:

```bash
humanlint init . --ux-qa --ci github --tools all --yes
npm ci
npm run build
humanlint ux audit --config ux-qa.config.toml
humanlint audit . --mode ratchet
```

and get a useful result without reading the paper first.

---

# Paper upgrades to match the product

The paper is compelling, but it should become more install-first and less manifesto-only.

I would add or rewrite these sections:

## 1. “The Humanlint Contract in 90 Seconds”

Put this near the front:

```md
Install:

cargo install humanlint
humanlint init . --tools all --ci github --ux-qa
humanlint audit . --mode advisory

This creates:
- AGENTS.md
- agent/HUMANLINT_STANDARD.md
- owner/test/proof maps
- IDE-specific agent adapters
- CI audit workflow
- optional UX QA config
- repair queue
```

## 2. “Strings Are Not Evidence”

Make this a named principle.

```md
A repository does not pass because it says it has tests.
It passes because the proof lane exists, runs, and emits artifacts.
Documentation is guidance, not proof.
```

This directly addresses the current scanner weakness.

## 3. “Default Stack, Not Universal Dogma”

Keep the strong recommendation:

```text
Rust + TypeScript/React/Vite + PostgreSQL + generated contracts + bounded Python
```

But define it as:

> the best-known default for agent-optimized product repositories, not a universal mandate.

Then say alternate stacks can pass if they satisfy the same control-plane requirements:

```text
agent passport
owner/test map
proof lanes
generated-zone protection
security lane
UX lane when UI exists
ratchet
issue queue
```

That will improve adoption because teams with unusual constraints will not feel rejected immediately.

## 4. “Agent IDE Support Matrix”

Add a table:

| Tool        | Primary file                        | Scoped/project files                     | Skill/workflow support      |
| ----------- | ----------------------------------- | ---------------------------------------- | --------------------------- |
| Codex       | `AGENTS.md`                         | nested `AGENTS.md`                       | `.agents/skills/*/SKILL.md` |
| Cursor      | `.cursor/rules/*.mdc` + `AGENTS.md` | rule-specific files                      | generated Humanlint rules   |
| Claude      | `CLAUDE.md`                         | `.claude/*`                              | `.claude/skills/*/SKILL.md` |
| Gemini      | `GEMINI.md`                         | hierarchical context files               | generated Humanlint context |
| Copilot     | `.github/copilot-instructions.md`   | `.github/instructions/*.instructions.md` | `.github/skills/*/SKILL.md` |
| Antigravity | rules/workflows adapter             | project rules                            | skills/workflows adapter    |

## 5. “Empirical Calibration Plan”

The paper currently has a strong rubric. To become a global standard, add a benchmark plan:

```text
Run Humanlint on 100 repos:
- 25 Rust/TS web apps
- 25 Python-heavy apps
- 25 JS/TS apps
- 25 mixed enterprise repos

Measure:
- false positive rate
- false negative rate
- audit duration
- agent repair success rate
- PR cycle time
- number of repeated agent mistakes
- score delta after one repair loop
```

Then publish:

```text
docs/benchmarks/
docs/calibration/
```

This turns the paper from “opinionated” into “ratcheting standard.”

---

# Immediate no-regret commits

I would implement these before anything else:

```text
1. Add Cargo.lock.
2. Add package-lock.json.
3. Fix paper asset fallback or include missing assets.
4. Remove AppleDouble files from archive.
5. Replace echo security lane with real commands or mark as TODO/advisory.
6. Add evidence scopes so docs cannot satisfy operational proof.
7. Add humanlint init skeleton.
8. Add SARIF + repair_queue.jsonl.
9. Add ux-qa.config.toml support.
10. Add release archive smoke test.
```

---

# Minimal implementation order for an agent

Hand this to an implementation agent:

```md
Implement Humanlint v0.3 around three deliverables:

1. Agent Passport Installer
   - Add `humanlint init`.
   - Generate root AGENTS.md, agent/*.toml/json, tool adapters, GitHub workflow, UX QA config.
   - Must be idempotent and support --dry-run.

2. Proof Graph Auditor
   - Add evidence scopes: RuntimeSource, OperationalConfig, GeneratedArtifact, Documentation.
   - Refactor rules so docs cannot satisfy operational proof.
   - Add hard/soft policy modes, SARIF, repair_queue.jsonl, and ratchet baseline.
   - Add regression fixtures for docs-only false positives and echo-security false positives.

3. Proofpack CI + UX QA Runtime
   - Add action.yml.
   - Add ux-qa config/discovery/reporters.
   - Fix release packaging, lockfiles, paper asset fallback.
   - Add release archive smoke test.

Acceptance:
- `npm ci` works.
- paper builds.
- no `._*` in release archive.
- `humanlint init --yes --tools all --ci github --ux-qa` works in temp repo.
- `humanlint audit --mode advisory` produces JSON, Markdown, SARIF, and repair queue.
- docs-only claims do not satisfy proof.
- echo commands do not satisfy proof.
```

My strongest recommendation: **make Upgrade 1 the headline, Upgrade 2 the trust engine, and Upgrade 3 the adoption engine.** The paper can stay ambitious, but the project wins when an agent can enter any repo, run `humanlint init`, read the generated passport, run `humanlint audit --changed`, and know exactly what to fix next.

[1]: https://developers.openai.com/codex/guides/agents-md "Custom instructions with AGENTS.md – Codex | OpenAI Developers"
[2]: https://docs.github.com/en/copilot/how-tos/copilot-on-github/customize-copilot/customize-cloud-agent/add-skills "Adding agent skills for GitHub Copilot - GitHub Docs"
[3]: https://github.com/gastownhall/beads "GitHub - gastownhall/beads: Beads - A memory upgrade for your coding agent · GitHub"
[4]: https://nexte.st/ "Home - cargo-nextest"
[5]: https://blog.rust-lang.org/inside-rust/2020/01/23/Introducing-cargo-audit-fix-and-more/?utm_source=chatgpt.com "cargo-audit v0.11: Introducing the `fix` feature, yanked ..."
[6]: https://crates.io/crates/cargo-deny?utm_source=chatgpt.com "cargo-deny - crates.io: Rust Package Registry"
[7]: https://mozilla.github.io/cargo-vet/ "Introduction - Cargo Vet"
[8]: https://github.com/bnjbvr/cargo-machete "GitHub - bnjbvr/cargo-machete: Remove unused Rust dependencies with this one weird trick! · GitHub"
[9]: https://github.com/geiger-rs/cargo-geiger "GitHub - geiger-rs/cargo-geiger: Detects usage of unsafe Rust in a Rust crate and its dependencies. · GitHub"

