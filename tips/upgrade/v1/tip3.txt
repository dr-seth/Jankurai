I studied the uploaded source archive closely. My strongest diagnosis: **the paper has the right thesis, but the project is not yet installable or enforceable enough to become a global standard.** The missing piece is not more prose; it is a **one-command bootstrap**, a **proof-grade audit that cannot be fooled by vibes**, and a **single executable stack template with boundary/UX proof built in**.

I’d make these the **top 3 upgrades**.

---

## 1. **Zero‑Scatter Bootstrap**

**Goal:** make Humanlint adoptable in one command, across all agent IDEs, without humans deciding where instructions belong.

Right now the repo has strong standard files, but adoption is still manual. `README.md` tells users to copy `agent/HUMANLINT_STANDARD.md`, create an `AGENTS.md` pointer, and run the audit. That is not enough for broad adoption. The standard should install itself into the known agent-helping locations and keep those files synchronized.

This matters because different coding agents now have different native memory/instruction locations. Codex uses `AGENTS.md`, discovers files from the repo root down to the working directory, and gives closer files precedence; OpenAI also recommends keeping these files concise and pointing to task-specific markdown when needed. Claude Code uses `CLAUDE.md`/`.claude/CLAUDE.md` and supports path-specific memory. GitHub Copilot supports `.github/copilot-instructions.md` plus path-specific `.github/instructions/*.instructions.md` files. Gemini CLI uses `GEMINI.md`. Antigravity-style workflows use `.agents` directories for workflows and skills. The open `AGENTS.md` convention is also explicitly meant to be a simple “README for agents.” ([OpenAI Developers][1])

### New command

Add:

```bash
humanlint init
```

With useful variants:

```bash
humanlint init --yes
humanlint init --dry-run
humanlint init --interactive
humanlint init --profile rust-ts-vite-react-postgres
humanlint init --agents codex,claude,cursor,copilot,gemini,antigravity
humanlint init --ci github
humanlint init --install-ux-qa
humanlint doctor
humanlint adapters write
```

The command should do three things:

1. **Detect the repo shape.**
   It should inspect `Cargo.toml`, `package.json`, Vite config, TS config, Python files, `db/`, `migrations/`, `contracts/`, `.github/workflows`, existing agent files, and monorepo layout.

2. **Generate the Humanlint standard files.**
   It should create or merge these:

   ```text
   AGENTS.md
   agent/HUMANLINT_STANDARD.md
   agent/audit-policy.toml
   agent/owner-map.json
   agent/test-map.json
   agent/proof-lanes.toml
   agent/generated-zones.toml
   agent/standard-version.toml
   docs/architecture.md
   docs/boundaries.md
   docs/testing.md
   docs/exceptions/README.md
   ```

3. **Generate IDE/agent adapter files from one canonical source.**
   These files should not contain divergent advice. They should point back to the canonical Humanlint standard.

   ```text
   AGENTS.md
   CLAUDE.md
   .claude/CLAUDE.md
   .cursor/rules/humanlint.mdc
   .cursor/rules/rust-core.mdc
   .cursor/rules/web-ui.mdc
   .github/copilot-instructions.md
   .github/instructions/humanlint-rust.instructions.md
   .github/instructions/humanlint-web.instructions.md
   GEMINI.md
   .agents/agents.md
   .agents/skills/humanlint/SKILL.md
   .agents/workflows/humanlint-audit.md
   ```

### The installer should be interactive, but not chaotic

When the repo layout is messy, `humanlint init --interactive` should produce a patch plan instead of silently imposing structure.

Example:

```text
Humanlint found:
- Vite app in ./src
- Rust API code in ./server
- SQL migrations in ./migrations
- No owner map
- No proof lane map
- Existing CLAUDE.md with conflicting test command

Recommended alignment:
- Treat ./src as apps/web
- Treat ./server as apps/api
- Treat ./migrations as db/migrations
- Add local AGENTS.md files for web/api/db boundaries
- Preserve existing paths with aliases instead of moving files

Apply?
[ ] Write agent files
[ ] Write audit policy
[ ] Write owner/test/proof maps
[ ] Add CI workflow
[ ] Move directories to canonical layout
```

Default behavior should be conservative:

```bash
humanlint init --dry-run
```

should write nothing.

```bash
humanlint init --yes
```

should create config, docs, CI, and adapters, but **not move application code** unless explicitly told:

```bash
humanlint init --migrate-layout
```

### Files to add or change

Add:

```text
crates/humanlint/src/init/mod.rs
crates/humanlint/src/init/detect.rs
crates/humanlint/src/init/plan.rs
crates/humanlint/src/init/apply.rs
crates/humanlint/src/init/prompts.rs
crates/humanlint/src/init/templates.rs
crates/humanlint/src/init/merge.rs

crates/humanlint/src/adapters/mod.rs
crates/humanlint/src/adapters/codex.rs
crates/humanlint/src/adapters/claude.rs
crates/humanlint/src/adapters/cursor.rs
crates/humanlint/src/adapters/copilot.rs
crates/humanlint/src/adapters/gemini.rs
crates/humanlint/src/adapters/antigravity.rs

templates/standard/agent/HUMANLINT_STANDARD.md
templates/standard/agent/audit-policy.toml
templates/standard/agent/owner-map.json
templates/standard/agent/test-map.json
templates/standard/agent/proof-lanes.toml
templates/standard/agent/generated-zones.toml

templates/adapters/codex/AGENTS.md
templates/adapters/claude/CLAUDE.md
templates/adapters/cursor/humanlint.mdc
templates/adapters/copilot/copilot-instructions.md
templates/adapters/copilot/humanlint-rust.instructions.md
templates/adapters/copilot/humanlint-web.instructions.md
templates/adapters/gemini/GEMINI.md
templates/adapters/antigravity/.agents/agents.md
templates/adapters/antigravity/.agents/skills/humanlint/SKILL.md
templates/adapters/antigravity/.agents/workflows/humanlint-audit.md

templates/ci/github/humanlint.yml
templates/ci/github/humanlint-strict.yml
```

Change:

```text
crates/humanlint/src/main.rs
README.md
AGENTS.md
docs/agent-native-standard.md
docs/release-plan.md
paper/tex/sections/06_humanlint_standard.tex
paper/tex/sections/10_agent_controls.tex
```

### Recommended Rust crates

Use:

```text
clap          CLI subcommands
dialoguer or inquire   interactive prompts
globset       path matching
ignore        fast repo walking
camino        UTF-8 path handling
minijinja or include_dir   embedded templates
similar       patch preview/diff rendering
toml          config read/write
serde_json    owner/test map read/write
serde_yaml    GitHub workflow generation
```

### Tests to add

Create fixtures under:

```text
crates/humanlint/tests/fixtures/init/
```

Add tests:

```text
blank_repo_generates_standard_files
existing_agents_file_gets_pointer_not_duplicate
dry_run_writes_nothing
yes_mode_is_non_interactive
cursor_adapter_contains_canonical_pointer
copilot_instruction_globs_match_owner_map
claude_file_stays_short
gemini_file_stays_short
antigravity_agents_directory_generated
messy_vite_rust_repo_gets_layout_plan
migrate_layout_requires_explicit_flag
```

### Success bar

After this upgrade, a new user should be able to run:

```bash
humanlint init --yes --ci github --install-ux-qa
humanlint audit . --fail
```

and get a repo that is agent-readable in Codex, Claude, Cursor, Copilot, Gemini, Antigravity-style tools, and terminal agents without manually copying files.

---

## 2. **Evidence‑Grade Audit Core**

**Goal:** make the audit fast, hard to fool, policy-aware, and directly actionable.

This is the most important technical upgrade.

The current audit is promising, but it has several release-blocking weaknesses in the uploaded archive:

* `repo-score.md` shows **score 72** but also says **“No findings”** and an empty Agent Fix Queue. A score below the policy floor should never produce zero actions.
* `agent/audit-policy.toml` defines a minimum score and fail conditions, but the Rust audit does not appear to enforce it.
* `.github/workflows/humanlint-audit.yml` only echoes several security tool names. The audit treats those strings as evidence, so placeholder text can falsely satisfy security checks.
* `Justfile` has a `security` recipe that only echoes tools instead of running them.
* The archive lacks `Cargo.lock` and `package-lock.json`, while the docs/CI use `--locked` and `npm ci`.
* `crates/humanlint/src/audit/mod.rs` is very large, but the audit excludes `crates/humanlint/` from product-code checks, so the project avoids its own shape rules.
* Generated-contract detection is too loose; any file containing “generated” can help satisfy the check.
* Owner/test/generated-zone manifests exist, but the audit mostly uses hardcoded heuristics instead of validating those manifests.
* Report output is not yet agent-optimal: findings need stable fingerprints, next commands, exception templates, affected owners, CI annotations, and issue export.

### New command set

Add:

```bash
humanlint audit . --policy agent/audit-policy.toml --fail
humanlint audit . --changed --json agent/repo-score.json --md agent/repo-score.md
humanlint ci . --policy agent/audit-policy.toml --sarif humanlint.sarif
humanlint ci . --baseline agent/humanlint-baseline.json --ratchet
humanlint issues export --format ndjson --out agent/fix-queue.ndjson
humanlint issues export --format beads --out .beads/humanlint.jsonl
humanlint issues export --format github --out .github/humanlint-issues.md
```

### Hard checks vs soft checks

Make this explicit in the standard.

**Hard checks** should block CI:

```text
No root AGENTS.md or equivalent agent entrypoint
Missing Humanlint standard file
Audit policy not enforced in CI
Score below policy floor
Security lane is placeholder/echo-only
Missing lockfile when install commands require one
Generated contract/code changed without manifest or regeneration command
Secret-like material in repo
Prompt-injection or overbroad agent-agency language in trusted files
Direct DB access in UI/domain/Python product code
Python used for product truth/authz/billing instead of ML/eval boundary
Destructive migration without rollback/backfill/lock evidence
Focused/skipped/tautological tests
Critical web UI surface without rendered UX proof
```

**Soft checks** should create warnings and fix queue items:

```text
Files approaching size limits
Weak names
Duplicated logic below hard threshold
Unmapped ownership
Unmapped tests
Long context files
Stale docs
TODOs without owner/expiration
Non-critical UX geometry issues
Dependency sprawl
Missing observability receipt
```

### Rule registry

Split the audit into a rule system.

Add:

```text
crates/humanlint/src/audit/rules/mod.rs
crates/humanlint/src/audit/rules/context.rs
crates/humanlint/src/audit/rules/ownership.rs
crates/humanlint/src/audit/rules/proof.rs
crates/humanlint/src/audit/rules/security.rs
crates/humanlint/src/audit/rules/contracts.rs
crates/humanlint/src/audit/rules/data.rs
crates/humanlint/src/audit/rules/shape.rs
crates/humanlint/src/audit/rules/ui.rs
crates/humanlint/src/audit/rules/python.rs
crates/humanlint/src/audit/rules/observability.rs
crates/humanlint/src/audit/rules/generated.rs
crates/humanlint/src/audit/rules/migrations.rs
```

Each rule should produce:

```rust
pub struct Finding {
    pub id: String,
    pub title: String,
    pub severity: Severity,
    pub hard: bool,
    pub confidence: Confidence,
    pub path: Option<PathBuf>,
    pub line: Option<u32>,
    pub owner: Option<String>,
    pub lane: Option<String>,
    pub evidence: Vec<Evidence>,
    pub why_it_matters: String,
    pub suggested_fix: String,
    pub suggested_commands: Vec<String>,
    pub exception_template: Option<String>,
    pub fingerprint: String,
}
```

Add confidence levels:

```text
exact       parsed config / AST / lockfile / CI command
strong      direct file/path evidence
heuristic   text-risk signal
```

A rule that only has heuristic evidence should usually be soft unless the pattern is dangerous enough.

### Make the audit hard to spoof

The audit should not count this as a security lane:

```yaml
- run: echo "cargo audit"
- run: echo "gitleaks detect"
```

It should require either:

```yaml
- run: cargo audit
```

or:

```yaml
- run: just security
```

where `just security` actually invokes tools, not echoes them.

For Rust dependency risk, add actual support for the Rust security tooling ecosystem. `cargo-audit` audits `Cargo.lock` against the RustSec advisory database; `cargo-deny` can check advisories, license policy, crate bans, sources, and duplicate versions; `cargo-vet` helps teams record third-party dependency audits; `cargo-nextest` is a faster Rust test runner with per-test isolation and CI support. ([RustSec][2])

Recommended security lane:

```bash
cargo audit
cargo deny check advisories bans licenses sources
cargo vet
cargo nextest run --workspace
cargo llvm-cov --workspace --fail-under-lines 70
cargo machete
cargo semver-checks
gitleaks detect --source .
syft . -o spdx-json=sbom.spdx.json
grype sbom:sbom.spdx.json
zizmor .github/workflows
actionlint
```

Do not require every repo to run every tool on day one. But the audit should distinguish:

```text
missing
declared-only
runs-locally
runs-in-ci
runs-and-blocks
```

### Report format

The report should be optimized for both humans and agents.

Add to JSON:

```json
{
  "schema_version": "humanlint.audit.v1",
  "standard_version": "0.3.0",
  "policy": {
    "path": "agent/audit-policy.toml",
    "minimum_score": 85,
    "decision": "block"
  },
  "score": {
    "raw": 72,
    "final": 72,
    "previous": 70,
    "delta": 2
  },
  "blocking_reasons": [
    "score below policy floor",
    "security lane is placeholder-only"
  ],
  "findings": [],
  "agent_fix_queue": [],
  "commands": {
    "fast": "just fast",
    "audit": "humanlint audit . --fail",
    "security": "just security"
  }
}
```

Each finding should include a directly usable repair recipe:

```json
{
  "id": "HLT-SEC-004",
  "title": "Security lane is declared but not executed",
  "severity": "high",
  "hard": true,
  "confidence": "exact",
  "path": ".github/workflows/humanlint-audit.yml",
  "why_it_matters": "Echoed tool names create false green CI.",
  "suggested_fix": "Replace echo placeholders with actual cargo-audit, gitleaks, SBOM, grype, zizmor, and actionlint steps.",
  "suggested_commands": [
    "just security",
    "humanlint audit . --fail"
  ],
  "exception_template": "Not allowed for this rule.",
  "fingerprint": "..."
}
```

### Integrations

Add issue export without requiring GitHub:

```text
agent/fix-queue.ndjson
agent/fix-queue.md
.humanlint/issues/*.md
.beads/humanlint.jsonl
```

Suggested `.beads` export shape:

```json
{
  "source": "humanlint",
  "rule_id": "HLT-SEC-004",
  "title": "Security lane is declared but not executed",
  "severity": "high",
  "status": "open",
  "path": ".github/workflows/humanlint-audit.yml",
  "line": 18,
  "owner": "platform",
  "lane": "security",
  "fingerprint": "..."
}
```

### Speed improvements

The audit should be extremely fast. Right now it repeatedly scans file text for different dimensions. Move to one inventory pass:

```text
1. Walk repo once with ignore.
2. Classify paths once.
3. Read text files once.
4. Store lowercase text once.
5. Run pattern packs with Aho-Corasick or compiled regex sets.
6. Run rules in parallel with rayon.
7. Cache file hashes optionally under target/humanlint/cache.json.
```

Add performance targets:

```text
small repo: < 250 ms
medium repo: < 2 s
large monorepo changed-mode: < 2 s
large monorepo full scan: < 15 s
```

### Files to add or change

Change heavily:

```text
crates/humanlint/src/audit/mod.rs
crates/humanlint/src/render.rs
crates/humanlint/src/model.rs
crates/humanlint/src/main.rs
.github/workflows/humanlint-audit.yml
Justfile
agent/audit-policy.toml
docs/audit-rubric.md
docs/testing.md
README.md
```

Add:

```text
crates/humanlint/src/policy.rs
crates/humanlint/src/ci.rs
crates/humanlint/src/issues.rs
crates/humanlint/src/sarif.rs
crates/humanlint/src/audit/inventory.rs
crates/humanlint/src/audit/classify.rs
crates/humanlint/src/audit/evidence.rs
crates/humanlint/src/audit/rules/*.rs
crates/humanlint/src/audit/perf.rs
crates/humanlint/tests/fixtures/bad_repos/*
crates/humanlint/tests/fixtures/good_repos/*
```

### Tests to add

```text
score_below_policy_floor_blocks
low_dimension_creates_fix_queue_item
echoed_security_tools_do_not_count
just_security_echoes_do_not_count
actual_security_commands_count
missing_cargo_lock_blocks_when_locked_install_used
missing_package_lock_blocks_when_npm_ci_used
owner_map_is_parsed_not_hardcoded
test_map_is_parsed_not_hardcoded
generated_file_without_manifest_blocks
generated_contract_without_regen_command_blocks
direct_db_access_in_web_blocks
direct_db_access_in_domain_blocks
python_product_truth_blocks
python_ai_service_allowed
destructive_migration_without_rollback_blocks
focused_test_blocks
skipped_test_blocks
tautological_test_blocks
prompt_injection_in_agent_file_blocks
overbroad_agent_agency_blocks
sarif_output_validates
beads_export_contains_fingerprints
changed_mode_still_checks_global_policy_files
ten_thousand_file_fixture_finishes_under_budget
```

### Paper improvements

Update the paper to say:

```text
A standard is not a document until it is executable.
A security lane is not evidence unless the tool actually runs.
A low score without a repair queue is a failed audit.
Agent files must be generated from one source of truth.
```

Touch:

```text
paper/tex/sections/06_humanlint_standard.tex
paper/tex/sections/10_agent_controls.tex
paper/tex/sections/11_continuous_proof.tex
paper/tex/sections/13_security_permissions.tex
paper/tex/sections/15_migration_governance.tex
```

---

## 3. **One‑Stack Proofpack**

**Goal:** stop stack scatter by shipping one blessed executable layout: Rust core/API, Vite/TypeScript/React UI, Postgres truth, Python only for ML/eval boundaries, plus deterministic UX QA.

The paper already argues for a winner architecture. The project should now make that architecture installable and auditable.

### Canonical layout

Add a stack template:

```text
templates/stack/rust-ts-vite-react-postgres/
  apps/
    web/
      AGENTS.md
      package.json
      tsconfig.json
      vite.config.ts
      playwright.config.ts
      ux.routes.json
      src/
      tests/
    api/
      AGENTS.md
      Cargo.toml
      src/
  crates/
    domain/
      AGENTS.md
      src/lib.rs
      src/error.rs
      tests/properties.rs
    application/
      AGENTS.md
      src/lib.rs
    adapters/
      AGENTS.md
      src/postgres.rs
    workers/
      AGENTS.md
      src/lib.rs
  contracts/
    AGENTS.md
    openapi.yaml
    generated/
  db/
    AGENTS.md
    migrations/
    seeds/
  python/
    ai-service/
      AGENTS.md
      pyproject.toml
      src/
      tests/
  ops/
    ci/
    security/
  docs/
    architecture.md
    boundaries.md
    testing.md
    decisions/
    exceptions/
    repair/
```

### Boundary rules

Make this table normative and generate local `AGENTS.md` files from it.

| Layer                | Owns                                                                                                      | Must not own                                                             |
| -------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `apps/web`           | React UI, route state, forms, local validation, generated API clients, component tests, Playwright, UX QA | Secrets, durable truth, authz, billing truth, direct DB access           |
| `apps/api`           | HTTP/RPC edge, request parsing, identity extraction, response mapping                                     | Domain invariants hidden in handlers, raw SQL sprawl                     |
| `crates/domain`      | IDs, invariants, state machines, pure decisions, typed domain errors                                      | IO, DB, env vars, framework code, time/random without injected interface |
| `crates/application` | Use cases, authz, idempotency, transactions, workflow orchestration                                       | UI details, SQL driver details                                           |
| `crates/adapters`    | Postgres, queues, external APIs, filesystem, env                                                          | Domain rules                                                             |
| `crates/workers`     | Background jobs, retries, replay, backoff                                                                 | Request/response UI concerns                                             |
| `contracts`          | OpenAPI/Protobuf/JSON schemas, generated clients                                                          | Hand-edited generated code                                               |
| `db`                 | Migrations, constraints, indexes, RLS, transactional truth                                                | Business workflows                                                       |
| `python/ai-service`  | ML inference, evals, embeddings, offline transforms                                                       | Product truth, authz, billing, direct production DB writes               |

The audit should enforce this with both path rules and import rules.

Examples:

```text
apps/web must not import pg, postgres, sqlx, diesel, Prisma DB clients, or server secrets.
crates/domain must not import sqlx, tokio-postgres, reqwest, std::env, std::fs.
python/ai-service must not import production DB clients unless marked as offline/eval and read-only.
contracts/generated must not be hand-edited.
```

### UX QA should be part of the standard, not optional prose

The uploaded `packages/ux-qa` package is a strong seed. It already checks deterministic rendered issues like target size, edge clearance, overlap, text clipping, button wrapping, horizontal overflow, sticky obstruction, z-index token use, focus visibility, labels, and nested scrollbars. The upgrade is to make it installable and connected to the audit.

Add:

```bash
humanlint ux init
humanlint ux audit --config agent/ux-qa.toml
humanlint ux audit --url http://localhost:5173 --out ux-qa.json
humanlint audit . --include-ux ux-qa.json
```

Generate:

```text
agent/ux-qa.toml
apps/web/ux.routes.json
apps/web/playwright.config.ts
apps/web/tests/ux.spec.ts
```

Example route matrix:

```json
[
  {
    "route_id": "home",
    "url": "http://localhost:5173/",
    "viewports": ["390x844", "768x1024", "1440x900"],
    "risk": "critical"
  },
  {
    "route_id": "settings",
    "url": "http://localhost:5173/settings",
    "viewports": ["390x844", "768x1024", "1440x900"],
    "risk": "high"
  }
]
```

The audit should require rendered UX proof for critical web surfaces:

```text
Critical route has no UX QA receipt       hard
Touch target below threshold              hard on critical route, soft elsewhere
Interactive overlap                       hard
Horizontal overflow                       hard
Text clipping                             hard on critical route, soft elsewhere
Missing focus visible                     hard
Missing form label                        hard
Raw z-index outside token scale           soft or hard by policy
Raw color/spacing outside token scale     soft initially
```

### CI profile

Generate a GitHub workflow that actually blocks.

```yaml
name: humanlint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  humanlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo tools
        run: |
          cargo install cargo-nextest --locked
          cargo install cargo-audit --locked
          cargo install cargo-deny --locked

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install JS deps
        run: npm ci

      - name: Fast proof
        run: just fast

      - name: UX proof
        run: just ux

      - name: Security proof
        run: just security

      - name: Humanlint audit
        run: humanlint ci . --policy agent/audit-policy.toml --sarif humanlint.sarif --fail

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: humanlint.sarif
```

Recommended `Justfile`:

```make
fast:
    cargo check --workspace --all-targets
    cargo nextest run --workspace --fail-fast
    npm run typecheck --workspaces --if-present
    npm run test --workspaces --if-present
    humanlint audit . --changed --policy agent/audit-policy.toml

ux:
    npm --workspace apps/web run build
    npm --workspace apps/web run preview &
    humanlint ux audit --config agent/ux-qa.toml --out agent/ux-qa.json

security:
    cargo audit
    cargo deny check advisories bans licenses sources
    cargo vet
    gitleaks detect --source .
    syft . -o spdx-json=agent/sbom.spdx.json
    grype sbom:agent/sbom.spdx.json
    zizmor .github/workflows
    actionlint

audit:
    humanlint audit . --policy agent/audit-policy.toml --fail
```

### Files to add or change

Add:

```text
templates/stack/rust-ts-vite-react-postgres/**
agent/ux-qa.toml
packages/ux-qa/templates/playwright.config.ts
packages/ux-qa/templates/ux.routes.json
packages/ux-qa/templates/ux.spec.ts
crates/humanlint/src/ux.rs
crates/humanlint/src/stack.rs
crates/humanlint/src/boundaries.rs
```

Change:

```text
packages/ux-qa/src/index.ts
packages/ux-qa/src/cli.ts
packages/ux-qa/package.json
package.json
Justfile
README.md
docs/boundaries.md
docs/testing.md
docs/architecture.md
paper/tex/sections/09_winner_architecture.tex
paper/tex/sections/12_pixel_qa.tex
```

### Tests to add

```text
stack_template_scores_above_85
stack_template_has_all_agent_files
web_direct_db_import_blocks
domain_sqlx_import_blocks
domain_env_import_blocks
adapter_domain_rule_blocks
python_ai_service_allowed
python_product_db_write_blocks
generated_contract_edit_blocks
ux_route_matrix_required_for_web_app
ux_audit_json_schema_stable
ux_critical_overlap_blocks
ux_missing_focus_blocks
ux_missing_label_blocks
ux_horizontal_overflow_blocks
```

---

# The biggest current gaps to fix before release

These are the concrete issues I would fix before polishing the paper further.

1. **Do not ship a stale or misleading score.**
   The archive’s `repo-score.md` says score 72 with no findings. That undermines trust. A score below 85 should always produce actionable findings.

2. **Do not count echoed commands as proof.**
   The current security lane placeholders create a false-green risk.

3. **Add lockfiles or stop using lockfile-dependent commands.**
   The archive does not include `Cargo.lock` or `package-lock.json`, but docs/CI use `--locked` and `npm ci`.

4. **Split `crates/humanlint/src/audit/mod.rs`.**
   A flagship anti-scatter project should not hide a 2,000+ line audit module outside its own shape rules.

5. **Make `agent/audit-policy.toml` real.**
   The policy file should control score floors, caps, hard checks, changed-mode behavior, and CI failure.

6. **Unify report location.**
   Some docs imply `agent/repo-score.json`; the archive has root `repo-score.json`. Pick one. I recommend:

   ```text
   agent/repo-score.json
   agent/repo-score.md
   agent/fix-queue.ndjson
   ```

7. **Make UX QA installable.**
   The UX package is promising, but it needs `humanlint ux init`, route config, CI integration, and a stable receipt schema.

8. **Add adapter files now, not later.**
   The paper names the right ecosystem. The project should generate those files immediately.

---

# Minimal implementation order for an agent

If an implementation agent starts with minimal context, give it this order.

## Phase 1: bootstrap

```text
1. Add humanlint init/doctor/adapters subcommands.
2. Embed templates.
3. Generate AGENTS.md, agent/*.toml/json, docs/*.md.
4. Generate CLAUDE.md, Cursor rules, Copilot instructions, GEMINI.md, .agents files.
5. Add dry-run and --yes modes.
6. Add tests for generated files and merge behavior.
```

## Phase 2: audit correctness

```text
1. Parse agent/audit-policy.toml.
2. Add policy decision to JSON/Markdown report.
3. Make score below floor produce blocking finding.
4. Make low dimensions produce fix queue items.
5. Reject echo-only proof commands.
6. Parse owner/test/generated-zone manifests.
7. Add SARIF output.
8. Add issue export.
9. Split audit/mod.rs into rule modules.
10. Add bad/good repo fixtures.
```

## Phase 3: stack + UX

```text
1. Add rust-ts-vite-react-postgres stack template.
2. Add boundary AGENTS.md files.
3. Add import/path boundary rules.
4. Add humanlint ux init/audit.
5. Add UX route matrix and receipt schema.
6. Connect UX receipt to humanlint audit.
7. Add CI template with fast/security/ux/audit lanes.
```

---

# My recommended final positioning

The project should not present itself as “another lint tool.” It should be positioned as:

> **Humanlint is the executable standard for agent-native repositories. It installs the agent memory files, defines the canonical stack boundaries, runs fast proof lanes, scores the repo, and gives agents a stable repair queue.**

The paper should sharpen around this sentence:

> **Humans scatter context; agents need stable routing. Humanlint turns a repo into a routable, auditable, repairable workspace.**

The best version of this project is not bigger. It is stricter, easier to install, and harder to fake.

[1]: https://developers.openai.com/codex/guides/agents-md "Custom instructions with AGENTS.md – Codex | OpenAI Developers"
[2]: https://rustsec.org/?utm_source=chatgpt.com "About RustSec › RustSec Advisory Database"

